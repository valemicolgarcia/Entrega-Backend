<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Iniciar Sesión</title>
    <link rel="stylesheet" href="/css/tailwind.css">
</head>

<body class="bg-pink-50">
    <!-- Contenedor que ocupa toda la pantalla y centra el formulario -->
    <div class="min-h-screen flex items-center justify-center">
        <!-- Forzamos un ancho máximo en línea (ahora 480px, el doble de 240px) -->
        <div class="bg-white p-8 rounded-lg shadow-lg w-full" style="max-width: 480px;">
            <h2 class="text-2xl font-bold text-center text-pink-600 mb-6">Iniciar Sesión</h2>
            <form action="/api/sessions/login" method="POST" class="space-y-4">
                <div>
                    <label for="email" class="block text-gray-700 font-semibold">Email:</label>
                    <input type="email" name="email" id="email" required class="mt-1 w-full border border-gray-300 rounded-lg px-3 py-2 
                   focus:outline-none focus:ring-2 focus:ring-pink-500 
                   bg-pink-100">
                </div>
                <div>
                    <label for="password" class="block text-gray-700 font-semibold">Contraseña:</label>
                    <input type="password" name="password" id="password" required class="mt-1 w-full border border-gray-300 rounded-lg px-3 py-2 
                   focus:outline-none focus:ring-2 focus:ring-pink-500
                   bg-pink-100">
                </div>
                <button type="submit"
                    class="w-full bg-pink-600 text-white py-2 rounded-lg hover:bg-pink-700 transition">
                    Iniciar Sesión
                </button>
            </form>
        </div>
    </div>
</body>

</html>