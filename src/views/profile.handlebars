<div class="bg-white p-8 rounded-lg shadow-lg max-w-md mx-auto mt-6">
    <h1 class="text-2xl font-bold text-pink-600 mb-2">
        Bienvenido, {{user.first_name}} {{user.last_name}}
    </h1>
    <p class="text-gray-700 mb-2">
        Email: <span class="font-semibold">{{user.email}}</span>
    </p>
    <p class="text-gray-700 mb-4">
        Estás logueado, viendo tu perfil
    </p>
    <form action="/api/sessions/logout" method="POST">
        <button type="submit" class="bg-pink-600 text-white px-4 py-2 rounded hover:bg-pink-700 transition">
            Cerrar Sesión
        </button>
    </form>
</div>