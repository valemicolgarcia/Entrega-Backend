<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/tailwind.css">
</head>

<body class="bg-pink-50 text-gray-800 font-sans" data-cart-id="{{cartId}}">
    <header class="bg-pink-600 p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <!-- Nombre de la tienda -->
            <a href="/" class="text-white text-2xl font-bold">Mi Tienda</a>

            <!-- Menú de navegación -->
            <nav class="flex space-x-4">
                <!-- Estos enlaces se muestran siempre -->
                <a href="/products"
                    class="bg-white text-pink-600 px-3 py-2 rounded hover:bg-pink-700 hover:text-white transition">
                    Productos
                </a>
                <a href="/mi-carrito"
                    class="bg-white text-pink-600 px-3 py-2 rounded hover:bg-pink-700 hover:text-white transition">
                    Mi Carrito
                </a>
                <a href="/api/sessions/current"
                    class="bg-white text-pink-600 px-3 py-2 rounded hover:bg-pink-700 hover:text-white transition">
                    Mi Perfil
                </a>

                {{#if usuario}}
                <!-- Si el usuario está logueado, muestra un círculo con su inicial -->
                <div class="inline-flex items-center justify-center text-white text-2xl font-bold px-2">
                    {{firstLetter usuario.first_name}}
                </div>





                {{else}}
                <!-- Si NO está logueado, muestra “Iniciar Sesión” y “Registrarse” -->
                <a href="/login"
                    class="bg-white text-pink-600 px-3 py-2 rounded hover:bg-pink-700 hover:text-white transition">
                    Iniciar Sesión
                </a>
                <a href="/register"
                    class="bg-white text-pink-600 px-3 py-2 rounded hover:bg-pink-700 hover:text-white transition">
                    Registrarse
                </a>
                {{/if}}
            </nav>
        </div>
    </header>

    <main class="container mx-auto p-6">
        {{{body}}}
    </main>

    <footer class="bg-pink-200 text-center p-4 mt-6">
        <p class="text-pink-700">© 2025 Perfumería. Todos los derechos reservados.</p>
    </footer>
</body>

</html>